---
title: Voorbeeld — controleerbare onderzoeksredenering
created: 2026-09-18
updated: 2026-09-18
type: example
status: active
tags: [pws, example, reasoning, evidence]
sources: []
---

# Voorbeeld: controleerbare onderzoeksredenering

> **Vormvoorbeeld met placeholders.** Dit document bevat geen echte deelnemers, resultaten of conclusie over N-Lang.

## Zwakke formulering

> N-Lang is makkelijker omdat de commando's Nederlands zijn en gebruikers de opdrachten sneller maakten.

Problemen: “makkelijker” is niet gedefinieerd, bron en meetopzet ontbreken, er is geen vergelijking, “sneller” heeft geen cijfers en de formulering verwart waarneming met oorzaak.

## Sterke opbouw

### 1. Bronfeit

**Geverifieerd — B`<nummer>`:** `<parafrase van relevante literatuurclaim>` (`<auteur, jaar, pagina/sectie>`). De bron gaat over `<populatie/context>` en is daarom `<wel/niet volledig>` vergelijkbaar met de gekozen N-Lang-doelgroep.

### 2. Vooraf gekozen meetcriterium

“Taakbegrip” wordt gemeten als `<operationele definitie>`. Het vooraf vastgelegde succescriterium is `<criterium>`. De keuze en onderbouwing staan in `onderzoekskader.md`.

### 3. Eigen waarneming

**Geverifieerd — dataset `<pad/ID>`:** in conditie A voltooiden `<n/N>` deelnemers de taak; in conditie B `<n/N>`. De mediane tijd was respectievelijk `<waarde + eenheid>` en `<waarde + eenheid>`. `<Aantal>` waarden ontbraken om `<reden>`.

### 4. Interpretatie

**Afgeleid:** binnen deze taak en steekproef hangt conditie `<A/B>` samen met `<voorzichtige beschrijving van verschil>`. De gegevens laten niet zonder meer zien dat de Nederlandse woorden de oorzaak zijn, omdat `<confound/beperking>`.

### 5. Deelconclusie

De resultaten ondersteunen deelvraag `<nummer>` `<wel/deels/niet>` voor `<exacte doelgroep en context>`. Zij rechtvaardigen geen uitspraak over `<bredere populatie, langetermijnleren of andere constructies>`.

## Tabelvoorbeeld

**Tabel X. `<Beschrijvende titel>`**

| Maat | Conditie A | Conditie B | Ontbrekend |
|---|---:|---:|---:|
| Taak voltooid | `<n/N>` | `<n/N>` | `<n>` |
| Mediane tijd (s) | `<waarde>` | `<waarde>` | `<n>` |
| Gemiddeld aantal hulpvragen | `<waarde>` | `<waarde>` | `<n>` |

*Bron: eigen onderzoek, dataset `<ID/pad>`, verzameld op `<datum>`. Beschrijf afronding en analysemethode in de methode.*

Verwijs in de lopende tekst naar Tabel X en leg uit wat zij bijdraagt; laat de tabel niet voor zichzelf spreken.
